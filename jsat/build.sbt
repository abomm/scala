import AssemblyKeys._ // put this at the top of the file

name := "jsat"

version := "1.0"

scalaVersion := "2.10.4"

assemblySettings

jarName in assembly := "jsat-assembly.jar"

//mainClass in assembly := Some("Jsat")